# Android Subsystem

This subsystem implements the Android personality on top of Bharat-OS.
It wraps the Linux base compatibility layer and adds distributed Binder,
Android process tagging, SELinux enforcement, and specialized shared memory endpoints.

For details, refer to `docs/android/ANDROID_PERSONALITY_ARCHITECTURE.md`.
