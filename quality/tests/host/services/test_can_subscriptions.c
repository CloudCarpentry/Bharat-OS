#include <stdio.h>
#include <assert.h>
#include <stdbool.h>

void test_mixed_subscriptions() {
    // mixed client subscriptions deliver only matching frames
    assert(true);
}

int main() {
    test_mixed_subscriptions();
    printf("test_can_subscriptions passed.\n");
    return 0;
}
